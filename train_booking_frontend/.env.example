# Frontend-Only Prototype Environment Variables
# No backend API URL needed - all data is stored in localStorage

# Vite Server Port (optional)
# VITE_PORT=8080

# Note: This is a frontend-only prototype.
# All data is stored in browser's localStorage.
# No external API or database connection required.
